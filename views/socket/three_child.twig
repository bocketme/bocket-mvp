<div class="row colla">
    <ul class="collapsible" data-collapsble="accordion">
        {% if node.children %}
            {% for child in node.children %}
                {% if child.type == TypeEnum.assembly %}
                    <li>
                        <div id="{{ child._id }}" class="collapsible-header three-node search_child valign-wrapper">
                            <span style="padding-left:{{ sub_level * 20 }}px"></span>
                            <i class="material-icons">keyboard_arrow_right</i>
                            <img class="pad-right" src="/img/07-Assembly icon.svg">
                            <span class="p-node" data-node="{{ TypeEnum.assembly }}" data-breadcrumbs="{{ child.breadcrumb }}" data-sublevel="{{ sub_level }}">{{ child.name }}</span>
                        </div>
                        <div id="{{ child._id }}-body" class="collapsible-body container valign-wrapper">
                        </div>
                    </li>
                {% else %}
                    <li>
                        <div id="{{ child._id }}" class="collapsible-header three-node valign-wrapper">
                            <span style="padding-left:{{ 30 + sub_level * 20 }}px"></span>
                            <img class="pad-right" src="/img/07-Part icon.svg">
                            <span class="p-node" data-node="{{ TypeEnum.part }}" data-breadcrumbs="{{ child.breadcrumb }}" data-sublevel="{{ sub_level }}">{{ child.name }}</span>
                        </div>
                    </li>
                {% endif %}
            {% endfor %}
        {% endif %}
    </ul>
</div>
{% if sockets %}
    {% for socket in sockets %}
        <script>
            console.log("{{ socket.message }}");
            socket.emit("{{ socket.order }}" {% for data in socket.dataToSend %} ,"{{ data }}" {% endfor %});
        </script>
    {% endfor %}
{% endif %}